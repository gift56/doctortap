import { PageHeading } from "@/components/page-heading/page-heading";

export default function ProviderPatientsPage() {
  return (
    <PageHeading
      title="Patients"
      description="Patient charts index, diagnostic logs, and clinical records."
    />
  );
}
