import { PageHeading } from "@/components/page-heading/page-heading";

export default function ProviderCalendarPage() {
  return (
    <PageHeading
      title="Calendar"
      description="Availability adjustments, recurring blocks, and sync settings."
    />
  );
}
