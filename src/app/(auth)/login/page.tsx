import { PageHeading } from "@/components/page-heading/page-heading";

export default function LoginPage() {
  return (
    <PageHeading
      title="Sign in"
      description="Single entry login for patients, providers, and administrators."
    />
  );
}
